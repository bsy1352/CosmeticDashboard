<!DOCTYPE html>
<html>
<head>
    <script src="~/three.js-r92/build/three.js"></script>
    <style>
        canvas{
            position:absolute;
            width:100%;

        }
    </style>
</head>
<body>
    <script src="~/three.js-r92/build/three.min.js"></script>
    <script src="~/three.js-r92/examples/js/controls/OrbitControls.js"></script>
    <script src="~/three.js-r92/examples/js/loaders/GLTFLoader.js"></script>
   
        <script type="module">
            import { GLTFLoader } from '~/three.js-r92/examples/js/loaders/GLTFLoader.js';

            let scene, camera, render


            function init() {

                scene = new THREE.Scene();

                camera = new THREE.PerspectiveCamera(40, window.innerWidth / window.innerHeight, 1, 5000);
                camera.rotation.y = 45 / 180 * Math.PI;
                camera.position.x = 800;
                camera.position.y = 100;
                camera.position.z = 1000;



                hlight = new THREE.AmbientLight(0x404040, 100);
                scene.add(hlight);

                directionalLight = new THREE.DirectionalLight(0xffffff, 100);
                directionalLight.position.set(0, 1, 0);
                directionalLight.castShadow = true;
                scene.add(directionalLight);

                light = new THREE.PointLight(0xc4c4cc4, 10);
                light.position.set(0, 300, 500);
                scene.add(light);

                light2 = new THREE.PointLight(0xc4c4cc4, 10);
                light.position.set(500, 100, 0);
                scene.add(light2);
                light3 = new THREE.PointLight(0xc4c4cc4, 10)
                light.position.set(0, 100, -500)
                scene.add(light3)

                light4 = new THREE.PointLight(0xc4c4cc4, 10);
                light.position.set(-5000, 300, 0);
                scene.add(light4);


                renderer = new THREE.WebGLRenderer({ alpha: true });
                renderer.setSize(window.innerWidth, window.innerHeight);

                controls = new THREE.OrbitControls(camera, renderer.domElement);
                $('#consdfs').append(renderer.domElement);


                let loader = new THREE.GLTFLoader()
                loader.load('3D_Objects/box.gltf', function (gltf) {
                    car = gltf.scene.children[0];
                    car.scale.set(0.5, 0.5, 0.5);
                    scene.add(gltf.scene);
                    animate();

                })
            }

            function animate() {
                renderer.render(scene, camera);
                requestAnimationFrame(animate);
            }
            init();

            //if (id !== null)
            //    cancelAnimationFrame(id);


            //var scene = new THREE.Scene();
            //var id = null;
            //var camera = new THREE.PerspectiveCamera(
            //75, window.innerWidth / window.innerHeight,
            //0.5, 1000
            //);
            //camera.position.z = 5;

            //var renderer = new THREE.WebGLRenderer({alpha: true});
            //renderer.setSize(window.innerWidth, window.innerHeight);
            //$('#consdfs').append(renderer.domElement);



            //var controls = new THREE.OrbitControls(camera);




            //Cube
            //var geometry = new THREE.BoxGeometry(1, 1, 1);
            //var material = new THREE.MeshNormalMaterial({ color: 0x00aaff });

            //var cube = new THREE.Mesh(geometry, material);
            //scene.add(cube);


            //function animate() {
            //    id = requestAnimationFrame(animate);


            //    renderer.render(scene, camera);

            //    controls.update();
            //}

            //animate();
        </script>
    <div id="consdfs" style="position:absolute">
    </div>

</body>
</html>
